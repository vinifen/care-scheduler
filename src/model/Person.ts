export default class Person {
    private name: string;
    private phone: number;

    constructor(name: string, phone: number, age: string) {
        this.name = name;
        this.phone = phone;
    }

}
